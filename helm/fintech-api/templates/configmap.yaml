apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "fintech-api.fullname" . }}-config
  labels:
    {{- include "fintech-api.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
