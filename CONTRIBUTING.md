# Contributing

1. Fork the repository and create a feature branch from `main`.
2. For Terraform changes: run `terraform fmt` and `terraform validate`.
3. For policy changes: run `conftest test policies/` to verify OPA rules.
4. Open a pull request describing the security or infrastructure change.

All contributions are welcome.
